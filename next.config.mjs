/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "existing-domain.com",
      "us-east-1-shared-usea1-02.graphassets.com",
    ],
  },
};

export default nextConfig;
